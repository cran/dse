# Expecting results from 1 tests

# -v8 -n400000  kills speed (on 16M memory machine)
# -v6 -n350000  dies with memory exhausted at test 12
# -v6 -n400000 227455 cons cells free (56%)  5453k bytes of heap free (88%)
#R --vsize=10M --nsize=300K
# gcinfo(1)
  require("dse2"); require("curve") #,  warn.conflicts=F)
 system.info()
 version.dse()
 dsecurvature.function.testsA(verbose=T, print.values=T) 
